import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      light: "#9061e2",
      main: "#5e35b1",
      dark: "#27057f",
    },
    info: {
      main: "#1F2022",
    },
  },
});

export default theme;
